type EarnedTimeInfo
  @model
  @auth(
    rules: [
      { allow: owner }
    ]
  ) {
  id: ID!
  userId: String!,
  carry_over_et: Float!,
  used_et: Float,
  current_hol: Float,
  hire_date_month: Int!,
  hire_date_day: Int!,
  hire_date_year: Int!,
  total_et_allowed: Float!,
  total_yearly_paychecks: Int!
}
